<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Connexion JDBC -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/gestion_affectations?serverTimezone=UTC&amp;useUnicode=true&amp;characterEncoding=UTF-8</property>
        <property name="connection.username">root</property>
        <property name="connection.password">root</property>

        <property name="connection.pool_size">5</property>

        <!-- Dialecte SQL -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Gestion de session -->
        <property name="current_session_context_class">thread</property>

        <!-- Affiche requêtes SQL générées -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- Génération/maj schéma -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Déclaration entités mappées -->
        <mapping class="model.Employe"/>
        <mapping class="model.Lieu"/>
        <mapping class="model.Affecter"/>

    </session-factory>
</hibernate-configuration>